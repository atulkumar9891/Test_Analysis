version: 2

exposures:
  - name: weekly_metrics
    label: customer data
    type: dashboard
    maturity: high
    description: For more exposure about project details

    depends_on:
      - ref('final')
      - ref('incremental_merge')
      - ref('order_delete_insert')
      - source('datafeed_shared_schema','stg_orders')
      - source('datafeed_shared_schema','stg_customers')
      - source('datafeed_shared_schema','stg_payments')

    owner:
      name: atul
      email: atul.kumar9891@gmail.com
